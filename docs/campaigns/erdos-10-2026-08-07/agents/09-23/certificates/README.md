# Certificates

Compact, independently checkable outputs belong here. Generators are untrusted. A certificate must state the exact claim, format, checker, and reproduction command.

The first certificate family records residue-sensitive finite closures and counterexample frontiers. Any future graph witness must include a canonical edge list and a complete max-cut certificate.