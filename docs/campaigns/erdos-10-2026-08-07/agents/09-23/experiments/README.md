# Experiments

This directory holds exact generators, checkers, and adversarial searches. Numerical optimizers are treated only as falsifiers unless their output is converted to an exact independently replayable certificate.

Planned additions:

- residue and frontier certificate generator/checker;
- structured max-cut counterexample search;
- exact tests of new gap-versus-slack inequalities;
- smallest-counterexample local configuration search.