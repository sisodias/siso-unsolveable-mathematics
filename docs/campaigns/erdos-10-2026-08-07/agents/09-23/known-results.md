# Known results reconstructed

1. The conjectured constant `1/25` is sharp, attained by balanced blow-ups of `C5`.
2. The canonical problem is equivalent to `a(N) <= floor(N^2/25)` at every order by exact blow-up.
3. The June 2026 flag-algebra work proves the canonical statement for `n <= 40`.
4. Conditional on the frozen exact upstream error value, strict integer rounding closes 147 normalized orders through 200; all `N <= 42` are closed.
5. A smallest counterexample may be assumed maximal triangle-free. At every frontier order left by the universal cap, it has exactly one unit of excess over `floor(N^2/25)`.
6. One-vertex insertion gives a minimum-degree floor near `4N/25` for a smallest counterexample.
7. For every triangle-free weighted graph with total vertex weight one, the fractional odd-cycle edge-cover optimum satisfies `Lambda <= 1/25`. The proof is elementary: construct a cover from reciprocal weighted degrees and use triangle-freeness on every odd cycle.
8. Fractional equality alone does not prove the conjecture: odd subdivisions can exhibit a genuine integral/fractional gap, although the known witnesses lie far below `1/25`.
9. Graphs whose relevant signed graph has no odd `K5` minor are covered by fractional integrality results; the unresolved region contains odd-`K5` obstructions.
10. Multiple fixed-cut averaging, global edge-density, odd-girth averaging, and small-grid slack routes have explicit counterexamples or retractions and are not to be reused without a new mechanism.